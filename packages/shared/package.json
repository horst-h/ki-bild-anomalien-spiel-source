{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"(noch keine Lint-Konfiguration)\"",
    "build": "echo \"(shared wird direkt aus src importiert, kein eigener Build nötig)\""
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
